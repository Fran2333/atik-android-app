<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/base_color_1"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/chef_image"
        style="@style/sc_content_style"
        android:layout_width="@dimen/photo_twidth"
        android:layout_height="@dimen/photo_theight"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.499"
        app:srcCompat="@drawable/chef" />

    <ImageView
        android:id="@+id/ingredients_image"
        android:layout_width="@dimen/photo_twidth"
        android:layout_height="@dimen/photo_theight"
        android:layout_marginStart="@dimen/margin_tt"
        android:src="@drawable/ingredientes"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/chef_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/cake_image"
        android:layout_width="@dimen/photo_twidth"
        android:layout_height="@dimen/photo_theight"
        android:layout_marginEnd="@dimen/margin_tt"
        android:src="@drawable/pastel"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/chef_image"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/progressBar2"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="@dimen/bs_tablet_width"
        android:layout_height="@dimen/bs_tablet_height"
        android:indeterminate="true"
        android:indeterminateTint="@color/bone_color"
        android:indeterminateTintMode="src_atop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/chef_image"
        app:layout_constraintVertical_bias="0.39999998" />


</androidx.constraintlayout.widget.ConstraintLayout>